import styled from "styled-components";

const Button = styled.button`
  padding: 0;
  cursor: pointer;
  appearance: none;
  border: none;
  outline: none;
  font-size: 1em;
  background: none;
`;

export default Button;
